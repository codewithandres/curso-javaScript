const arreglos = ['Sebastian', 'Tatiana', 'Orlando'];
console.log(arreglos[0]); // seleccionar arreglos

const colores = []; // aggregar valores alos arreglos
colores[0] = 'rojo';
colores[1] = 'verde';
colores[3] = 'azul';

console.log(colores.length);

colores.push('Amarillo') // agrega un valor al final del arreglo
